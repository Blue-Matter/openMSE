#' @param model An optional name for the model.
#' If `x` is a list of objects, `model` will be taken from `names(x)`.
#' If `names(x)` is `NULL`, `model` will be given sequential numerical values (e.g., `Model 1`, `Model 2`, ...)
