#' @param scale An optional function with a single numeric argument that returns transformed or scaled numeric values. See `lb2kg` and `kg2lb` for example.
#' Can be a list of functions for list objects (NA for no transformation)
